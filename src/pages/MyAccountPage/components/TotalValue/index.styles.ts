import { Stack } from "react-bootstrap";
import styled from "styled-components";

export const Container = styled.div`
  margin-left: 12px;
  margin-right: 12px;
`;

export const StocksValue = styled(Stack)`
  text-align: end;
`;